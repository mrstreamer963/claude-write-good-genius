_Закрытое из `bugs.md`, вторая порция (первая — в `bugs.md` этой же папки). Нумерация
сохранена исходная, поэтому в списках есть пропуски: незакрытое осталось в `bugs.md`._

## bugs
1. [x] проверить что с лапами - часто вижу у кота в карточке лапы 0/4 а груз он тем временем все равно несет (?)
   — разобрано: ядро тут ни при чём, панель и карта читают одно поле одного снапшота, груз выше
   предела лап не поднимается никогда. Расходится **кто выбран**: `unitAt` берёт первого кота из
   клетки, а клетку коты делят (идущий сквозь стоящего не разводится), плюс брусок груза нарисован
   на верхней границе своей клетки и читается как груз соседа сверху. То есть это UI, и чинится
   вместе с `extension-ui` №4.
2. [x] наблюдал ситуацию когда один кот "работал" (таскал предметы), остальные же просто стояли рядом
   — claim'ы подвоза убраны, работа делится по остатку (§12.48): кучу и площадку разбирают
   столько котов, на сколько хватает остатка.
3. [x] проблемы рядом со стелажом - несколько котов занимают одну клетку после остановки - это прямое нарушение! - возможно из-за того что по очереди приносили материалы к стелажу и становились неподалеку от него - больше не воспроизводится
   — замер на боевом рулсете (4000 тиков, стройка + уборка + приказы): двое **остановившихся**
   на одной клетке — 0 случаев, а вот проходящих сквозь стоящего — 77 тиков из 4000, и это
   законно (§12.32). Но стеллажа в том прогоне не было: он за «Материаловедением», а
   `clear_solids` в тупике сходит на **занятого** соседа намеренно (§12.39) — проверять надо
   отдельно, схемой со стеллажом в углу.
4. [x] новая партия должна начинаться всегда с ускорения времени x1
   — «Новая партия» сбрасывает темп на ×1 вместе с базой.
5. [x] штамповка деталей - не работает при нажатии кнопка "отменить"
   — причина: панели перерисовываются каждым снапшотом целиком, и обработчик висел на узле
   кнопки, который живёт один кадр. `click` требует `mousedown` и `mouseup` в одном элементе —
   между ними панель успевала перерисоваться. Теперь клик ловится делегированием на контейнере,
   один раз на всю партию; та же поломка была у «Бросить» в теме и «Отозвать» в вылазке.
6. [x] спящие коты при назначении на вылазку сразу просыпаются и идут - если не отключено правило "Беречь себя" пусть сначала спят спокойно а потом идут на вылазку. То же и с приказом
   игрока: спящий сначала досыпает, а потом идёт выполнять.
   — сделано (§12.51): `Sim::launch` и `Sim::set_target` больше не зовут `release_task` на
   спящего, пока включено «Беречь себя», и маршрута ему не трогают — пишут только своё (`Squad`,
   `Order`), а поведут кота `gather_squad` и `retry_orders` в тик пробуждения. Решение игрока не
   теряется и второго клика не требует: отряд ждёт бойца так же, как уже ждал истощённого
   (§12.23). Панель миссии при этом говорит «ждут, пока выспится боец», а карточка кота — «спит:
   приказ подождёт»: иначе ожидание без срока читается как поломка. Выключенное «Беречь себя»
   будит, как раньше.
7. [x] «не может дойти» стало видно чаще — приказ, потерявший маршрут не из-за карты (сон, рана,
   вылазка), больше не перепрокладывался никогда: `set_target` отмечал версию карты даже при
   найденном пути, а `retry_orders` по этой отметке пропускал кота. Теперь отметка ставится
   только на неудаче и снимается на удаче (`an_order_resumes_after_a_nap`).
8. [x] нельзя продать то, чего нет - механика продажи вещей которых пока на базе нет интересная (можно в будущем расширить на "контракт на производство" - к сроку предоставить N материалов, если не выполнил - штраф и снижение репутации) - но сейчас это блокирует всю торговлю, так что "фьючерсы" вне PoC
   — сделано (§12.50): заявку без товара не принимают, а принятая **бронирует** его — базе
   забронированное больше не принадлежит. Бронь читают все шесть мест, где предмет уходит с
   базы: подвоз на площадку, плата складом (найм, наука, заказ, аптечка), еда и снаряжение.
9. [x] вверху показывается общее количество предметов на базе включая кучки не на складе (?) а действия требуют предметов со склада - в итоге игрок видит что на базе все есть но не может
сделать действие, например,нанять кота, так как часть предметов лежит в кучках.
   — сделано (§12.53): ядро отдаёт три числа на тип (`stored`, `loose`, `booked`), в шапке
   главным идёт **то, чем можно платить** (склад минус бронь), а валяющееся приписано
   приглушённым «+N» с подсказкой «платить этим нельзя, пока не убрано». Отказ кнопки теперь
   называет числа: «Лом 3 из 12, ещё 9 валяется — уберите на склад». Заодно `storage_covers`
   стал вычитать бронь — раньше доступность действия и само списание считались по-разному.


## extension-ui
1. [x] чтобы кнопка "изучить: Наука" была у парты а не в глобальном меню (?) - посмотреть не повлияет это на целевой геймплей, чем занимается игрок - постоянный микроменеджмент кому куда идти или глобальное видение какие направления развивать и чем сейчас заниматься тем кто свободен ?
2. [x] чтобы торговля появлялась у кота который занимает торговый слот (?) - решение аналогично 1
3. [x] "штамповка деталей" - аналогично 1, когда кот находится на ячейке "производство" - с другой стороны игрок может отправить кота "на производство", а тот дойдет до места, увидит новую цель и пойдет сам ее выполнять цель - игрока это расстроит, так как кот сам "ушел" куда-то - "игра глючит"
   — все три разобраны вместе (§12.55), и ответ вышел не «да» и не «нет», а «наполовину».
   **Кнопки на клетках не будет**: ядро не адресует работу месту — `shop_spot` и `lab_spot`
   берут ближайшую свободную комнату к тому коту, которого выбрала симуляция, а игрок размечает
   работу и исполнителя не выбирает (§12.16). Кнопка «заказать здесь» обещала бы адресность,
   которой нет, и с двумя мастерскими врала бы через раз. Пункт 2 закрыт ещё жёстче: «кота,
   который занимает торговый слот», не существует — пост это лицензия, а не рабочее место
   (§12.44). Опасение из пункта 3 («кот сам ушёл») тем же и снято.
   **Зато сделано то, ради чего это спрашивалось.** Клик по функциональной клетке (а) показывает,
   что в ней происходит сейчас — какой заказ крутится на этом станке и кто за ним, какая тема в
   лаборатории, сколько сделок из скольких постов, — и (б) **раскрывает её раздел тулбара**:
   ткнул в мастерскую, открылось «Производство». Единый центр управления цел, а находимость
   появилась.
   И главное, что вылезло по дороге: вторая мастерская и второй пост **не делали ничего**.
   Теперь комната — это слот: заказов идёт столько, сколько мастерских, сделок — сколько постов
   (§12.55). Потолок производства и торговли стал строительным решением, то есть попал в петлю
   игры, а не остался числом в фасаде.
10. [x] штамповка деталей - сделать чтобы можно было одновременно производить разные типы ресурсов, если повторно нажали на кнопку производства - кол-воп планируемого к производству материала увеличивалось бы
   — сделано (§12.55), обе половины. Разные типы одновременно: заказов идёт столько, сколько
   мастерских, и каждый за своим станком (занятость держит `Craft::spot`, как лежанку держит
   `Rest::spot`). Повторное нажатие: заявка на **тот же** рецепт добавляет штук к уже
   размеченному заказу, а не заводит второй, — двух заказов на один рецепт не бывает, и поэтому
   же отмена ушла по рецепту, а не по номеру строки в панели.
11. [x] если кот без дела и потребностей - пусть он идет спать, но тут нюанс - спящий кот неподконтролен игроку - кот будет все время спать (?) - вводить планку бодрости выше которой кот может быть разбужен игроком ? вводить состояние "отдыхает" - когда занимает лежанку но не спит ?
   — сделано (§12.52). Дремота есть, и неподконтрольным кот больше не остаётся: выше потолка сна
   его поднимает и приказ, и любая появившаяся работа. К свободной лежанке кота отправляет
   `assign_nap` — последний раздатчик в цепочке («коту ничего не досталось» — факт только после
   всех остальных) — и ведёт **по шагу за тик**: с полным маршрутом кот на десятки тиков выпадал
   бы из раздачи, и освободившийся чертёж ждал бы, пока бригада дойдёт до спальни.
12. [x] уровни бодрости: 
   * A - бодрость меньше A - кот падает где стоял и засыпает пока бодрость не достигнет B, B > A, подобрать чтобы кот постоянно не просыпался и тут же засыпал
   * B - уровень бодрости который можно получить в местах не приспособленных для сна
   * C - уровень границы сна, C > B , если кот добрался до лежанки и заснул, он неподконтролен никому пока не выспится до уровня С
   * все что выше C - кот "дремлет" - доступен для игрока и раздачиков
   * конечно же все это работает при включеной опции "Беречь себя", при отключеной коты работают на износ
   — сделано (§12.52), с двумя правками по ходу. **A не заведён**: это ноль шкалы в других
   единицах, а гистерезис целиком даёт B. **B и C оказались одним понятием** — «докуда здесь
   высыпаются», — и живут свойством места рядом со скоростью: `wake` у тайла, `floor_wake` в
   `energy:`. В боевом рулсете пол — 600 (ниже `tired: 900`: там только отлёживаются, чтобы
   доползти до кровати), лежанка — 2400, гнездо высыпает целиком. Выше потолка `Rest` снят: кота
   видит любой раздатчик, поднимает приказ, и лежанку он не держит — её забирает тот, кому спать
   по нужде. Охват «Беречь себя» остался прежним (второй порог + §12.51): повесить на тумблер всю
   модель значило бы, что с выключенным коты спят только на полу и база молча теряет треть
   производительности, а кнопка обещает другое.
13. [x] кот мечется туда-сюда, стоя в клетке между стеллажами (трейс от 6.08); и второе: один
   кот дремлет на лежанке, второй без дела шагает на неё и откатывается обратно — «дёрганье»
   — причина одна (§12.52): дремлющий идёт к лежанке **по шагу за тик**, то есть замирает на
   каждой клетке маршрута, — а клеток, где пройти можно, а остаться нельзя, в игре две: полка
   (§12.35) и клетка под остановившимся котом (§12.32). Обе разбираются после факта, поэтому
   замершего там кота сгоняли тем же тиком, а раздача посылала его туда же снова. Теперь шаг
   растягивается до ближайшей клетки, на которой можно замереть (через ряд полок — два-три
   шага сразу). Ловит это `a_nap_crosses_a_rack_instead_of_bouncing`.
14. [x] опять не работают кнопки «Отменить» у заказа и «Отозвать» у вылазки (та же поломка, что
   и в №5, второй серией)
   — делегирования на контейнер оказалось мало. Узел кнопки живёт один кадр, а клик человека —
   сотню миллисекунд; браузер отдаёт `click` ближайшему общему предку нажатия и отпускания, то
   есть контейнеру, и `event.target` у такого события — **сам контейнер**, а не кнопка. Проверка
   `target.closest('.mission-cancel')` не находила ничего, и обработчик молчал. Теперь нажатие и
   отпускание разбираются по отдельности: `mousedown` взводит (его цель — живой узел под
   курсором), `mouseup` спускает, если под курсором свежий узел той же кнопки. Семантика клика
   цела: увёл курсор и отпустил в стороне — ничего не произошло.
15. [x] при нажатии на ячейку показывается состав этой ячейки - условно говоря "стеллаж", содержит "5 лом , 10 деталь"
   — сделано (§12.54). Панель клетки говорит про тайл и его роль, кучи по типам,
   заполненность склада, площадку с подвозом и про то, кто на клетке стоит. Ядру это не
   стоило ничего: кучи, площадки и свойства тайлов уже ехали в снапшоте.
   Мешал же не показ, а **ввод**: клик по полу с выбранным котом был сразу приказом, и
   осмотреть клетку, не двинув никого, было нельзя. Поэтому клик разбит надвое — первый
   показывает клетку, повторный по той же клетке отправляет туда котов. Дорожает ровно то,
   чем пользуются редко: §12.16 сделал приказ оговоркой, а не петлёй (уборка, снаряжение,
   сон и дремота — на котах), а осмотр нужен постоянно. Повторный клик по клетке
   единственного выбранного кота его **отпускает** — тем же жестом закрыт и запрос «повторный
   клик снимает выделение». Обещание второго шага видно заранее: «ещё клик сюда — пойдут: …»
   в панели и зелёные уголки на карте.
   Заодно видно, **кто** стоит на клетке: `unitAt` берёт первого кота, а клетку коты делят на
   проходе (§12.32) — на этом разошлись показания в №1, и спросить об этом было негде.
   Частично видна и беда extension-ui №5 (коты-невидимки на паузе): сколько их там, панель
   теперь называет поимённо, хотя нарисован по-прежнему один.
