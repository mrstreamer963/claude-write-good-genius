_Здесь только открытое. Закрытое уехало в `archive/bugs.md` (первая порция),
`archive/bugs-1.md` (вторая) и `archive/bugs-2.md` (третья); нумерация в этом файле начата
заново, но в списках есть пропуски — унесённое в архив сохранило свои номера, поэтому ссылки
вида «bugs №9» из `concept.md` смотрят в архив. `archive/bugs-3.md` — **не порция закрытого**,
а сырые заметки по вылазкам (этапы, отзыв из поля, вилка состава); часть из них закрыта §12.70._

_Статусы сверены с кодом 15.08 (на `97f95b5`). Пункт, у которого предпосылка успела измениться,
несёт строку «сверено 15.08» с тем, что от него осталось: анализы писались до §12.59–§12.77 и
местами описывают механику, которой больше нет. Непомеченный пункт верен как написан._

## bugs
Открытых нет.

## extension-ui
1. [ ] на карте два слоя - один слой "пол" на котором можно ставить различные девайсы, например пол "склад" , "стеллаж" - можно ставить только на нем. Как вариант - если на карту ставится стеллаж то автоматически "пол" под ним становится типом "склад" (?) Нет ли здесь усложнения логики игры ?
   — анализ: усложнение есть, и оно не в игре, а в протоколе. «Тайл — индекс палитры `tiles:`»
   ходит сейчас по всему проекту: карта, `BaseMap::set` и `version`, проходимость, BFS, снос,
   снапшот, сейв (индексы в снимке — числа), ASCII-схемы тестов, рендер. Второй слой — это
   второй индекс в каждом из этих мест, то есть цена уровня §12.45, а не украшение.
   Автопревращение пола под стеллажом дешевле, но это ровно тот «скрытый условный guard»,
   который §12.10 уже отверг: игрок ставит одно, а появляется другое, и объяснить это можно
   только текстом. Плюс правило «стеллаж только на складе» ничего не добавляет к балансу:
   стеллаж и так за «Материаловедением» и стоит 4 лома + 3 детали.
   Главное — под вопросом нет механической нужды: все «девайсы» рулсета уже тайлы, и ни один
   не хочет второго слоя. Настоящий спрос появится вместе с мебелью на несколько клеток с
   фасадом (её §12.39 уже отложил по своей причине) — тогда и решать, одним §12-решением.
   **Рекомендация: закрыть как «не на PoC», записав в §12 отвергнутым вариантом.**
2. [ ] на паузе непонятно сколько котов в одной ячейке - коты не могут занимать одну ячейку в конце движения (проверить - соблюдается ли это сейчас ?) но в режиме паузы получаются коты-невидимки - варианты решения ? рисовать несколько котов в одной ячейке ?
   — анализ: проверено ещё в архивном bugs №3 — двое **остановившихся** на одной клетке не
   встречаются (0 случаев за 4000 тиков), а проходящие сквозь стоящего законны (§12.32) и
   на паузе как раз и застывают. То есть это не баг симуляции, а дыра рендера, и после §12.54
   она наполовину закрыта: панель клетки называет всех поимённо.
   Осталась вторая половина, и она хуже первой: `unitAt` берёт **первого** кота клетки, значит
   второго мышью не выбрать вовсе — невидимка ещё и неуправляем. Чинится целиком в `main.js`:
   значок «×2» на клетке (или сдвиг спрайтов на пару пикселей) плюс перебор котов по кругу
   повторным кликом — жест уже занят «повторный клик = приказ» (§12.54), так что перебор
   вешать на панель клетки, где имена уже перечислены.
   **Цена низкая, ядро не трогается. Хороший кандидат на «сделать сейчас».**
   — сверено 15.08: в силе целиком. `unitAt` (`web/src/main.js:2317`) по-прежнему отдаёт первого
   из `unitTiles`, панель клетки перечисляет всех (`main.js:1289`), выбрать второго мышью нельзя.
3. [ ] что если продавать тоже отложенно - коты складируют груз на продажу в гараже (продажа - опция "с доставкой" ? появляется миссия "Отвести груз?" , покупка - опция "самовывоз" - миссия на доставку груза)
   — анализ: половина этого уже есть и работает именно так. Продажа **уже** отложенная, а с
   §12.68 — ровно в описанной форме: коты набивают товаром контейнер в ячейке торгового поста,
   срок отгрузки идёт с момента, когда он полон, деньги приходят разом по его истечении, бронь
   висит с заявки (§12.50). Покупка так же ложится кучей в ячейку поста. Новизна тут одна —
   **выбор** «с доставкой / самовывоз» и вылазка как способ этот выбор оплатить.
   Что для этого нужно завести: миссию, порождённую **сделкой**, а не рулсетом. Сейчас
   `MissionRules` — контент, а `Sim::launch` берёт `def: usize` из палитры; динамическая
   миссия — это новый источник миссий и новая ветка исхода (провал самовывоза = потерянный
   груз или потерянные деньги, и это надо решать явно, потому что §12.44 держит инвариант
   «торговля не создаёт богатства»).
   — **сверено 15.08: предпосылка снята, пункт стал делаемым.** «Одна вылазка за раз» кончилась
   в §12.59 (вылазок идёт столько, сколько узлов связи), поэтому самовывоз больше не отнимает
   единственную вылазку и ловушкой не становится. Остался ровно один настоящий вопрос, и он не
   про слоты: **миссия, порождённая сделкой, а не рулсетом** — второй источник заказов, его
   карточка в штабе (§12.71) и явная ветка провала.
6. [ ] походу, ячейка пола вмещает бесконечное число ресурсов - стоит ли делать ограничение (?) выглядит странно - кучка маленькая а помещается целый склад
   — анализ: цена у бесконечной кучи **уже есть** и работает — завал замедляет шаг (§12.35):
   8 штук = +1 тик, потолок +3, то есть куча в 24 штуки делает клетку вчетверо дороже
   (`MOVE_PERIOD = 1`). Странно выглядит не правило, а картинка: куча нарисована одинаково при
   3 и при 300.
   Жёсткий лимит дороже, чем кажется, и не в проверке. Кучи ложатся на пол из шести мест —
   добыча на шлюзе (§12.22), покупка в ячейку торгового поста (§12.68), готовое под ноги
   мастеру (§12.30), возврат с отменённой площадки (§12.31), ноша, роняемая заявкой на вылазку
   (§12.38), и снос, — и **все шесть сейчас безусловны**, потому что инварианты 8 и 11
   обещают, что ничего не исчезает. Лимит превращает каждое в поиск места и заводит новый
   отказной случай «класть некуда», которого в игре пока нет ни одного.
   Дешёвое решение той же жалобы: рисовать кучу ступенями по размеру (данные в снапшоте уже
   есть) и, если хочется наказывать сильнее, снять или поднять `CLUTTER_MAX` — тогда «склад
   посреди коридора» платит временем, а не запретом. Это ровно приём §12.35: правило после
   факта вместо запрета до него.
   **Рекомендация: ступени в рендере + при желании потолок завала; жёсткий лимит — только
   отдельным §12-решением с правилом переполнения.**
   — сверено 15.08: в силе; из шести мест переехало одно — купленное с §12.68 ложится в ячейку
   поста, а не на шлюз. Числа завала прежние (`CLUTTER_PER_TICK = 8`, `CLUTTER_MAX = 3`).

### design update
1. [ ] добавить в записку записи не ограниченные по времени (?) типа - "найми "Гвоздя" , он тебе пригодится...  если например "Гвоздь" не нанят и геймплейно исчезает, то задание становится проваленным
   — анализ: это не таймлайн. Событие §12.28 срабатывает **потому, что настал его тик**, у него
   ровно два исхода и оно только «случилось». Бессрочная запись с провалом — это цель (§12.58),
   у которой уже есть и ярлык, и подсказка, и два вида условий. Но она заведёт то, чего у целей
   нет намеренно: **отнимаемое состояние** — `Goals` только растёт, и на этом держится «ярлык
   пишется в прошедшем смысле». Провал придётся вводить третьим состоянием и решать, что с ним
   делает загрузка снимка. Ещё нужен исчезающий кандидат: сейчас `recruits:` не пропадают
   никогда. **Средняя цена, но требует пересмотра §12.58 — записывать вопросом в §12.**
3. [ ] задать автопродажу лишних ресурсов (задать достаточное кол-во ресурса на базе), остальное - продается автоматом (какой фракции? случайным образом ? смотреть на текущие цены всех фракции и продавать по максимальной цене ?)
   — анализ: та же форма, что и №4 в `extension-ui` (лимит + автоматика; пункт закрыт и уехал
   в `archive/bugs-2.md`), и выбор фракции
   решается без рандома: курс — чистая функция тика (§12.44), значит «по максимальной цене»
   детерминированно и вычислимо вперёд. Одна засада: сделка **не отменяется и бронирует товар**
   (§12.50), поэтому автопродажа умеет забронировать лом, который через тик понадобится
   стройке, — лимит обязан считаться по `stored − booked` и с запасом. **Делать только после
   автопроизводства: механизм общий, а рисков у продажи больше.**
   — **предпосылка снята: автопроизводство сделано (§12.65), и форма правила теперь описана
   (§12.64).** Ложится вторым ресурсом-порогом рядом с `Stocking` и второй системой рядом с
   `plan_craft`; из §12.64 сюда переезжает и «на одну разметку — один источник» (ручная сделка
   по тому же предмету должна выключать правило на время), и «правило пересчитывается каждый
   тик». Открытым остаётся ровно то, что названо выше: сделка не отменяется, поэтому порог
   продажи обязан считать по `stored − booked` и с запасом на стройку.
   — сверено 15.08: с §12.68–§12.69 у пункта уточнились два места. **Слот теперь не пост, а его
   ячейка** (`Sim::free_post_cell`, `src/sim.rs:571`): правило обязано спрашивать её, иначе
   заведёт сделку, которой некуда лечь. И **какую бронь вычитать, решает §12.50**, а не вкус:
   порог производства считает добро базы вместе с лапами (`on_base_counts`, `src/hauling.rs:1041`,
   форма `owed`), а ворота самой продажи берут из куч (форма `booked`) — у автопродажи в одном
   правиле встретятся обе, и это надо развести явно, а не «по аналогии».
4. [~] добавить в игру ачивки 
   — сделано по существу (§12.58), проверено по коду. Есть всё: `check_goals` в конце цепочки,
   три журнала совершённого (`Raids`, `Crafted`, `Earned`), восемь записей в `goals:` боевого
   рулсета (семь обязательных плюс скрытая «Своих не бросаем»), счётчик «цели 3/7» со
   сворачиваемой панелью и полосками прогресса, раздел «сверх того» для скрытых, тост на
   взятие и финальный модал. Взятое сохраняется вместе с партией (`Goals` в `SAVED`,
   `src/save.rs:116`), покрыто 21 тестом в `src/tests/goals.rs`.
   Открытыми остаются две мелочи, и обе — про **вид ачивки, а не про механику**:
   * **Нет класса «видна, но не в счёт».** Видов ровно два: обязательная (держит финал) и
     `hidden` (в снапшот не уходит, пока не взята). Ачивка в привычном смысле — третий:
     видна заранее, стремиться к ней можно, а финал она не держит. Это один флаг в `GoalRule`;
     панель к нему уже готова — знаменатель считается отдельно (`goals_required`), а не по
     длине списка.
   * **Нет копилки между партиями.** `Goals` живёт в снимке, «Новая партия» обнуляет его. Если
     ачивки должны переживать забег — это `localStorage` на стороне вида, а не ядро: то, что
     не влияет на симуляцию, в ядро не лезет (§12.46).
   **Решать надо не «делать ли систему», а нужен ли третий вид цели вообще.**
5. [ ] игрок начинает с разрушеной базы, которую разбирает на лут, и строит свою - придется добавить новые материалы, которые нужно переработать (переплавка?) (либо продать задешево?) - "ржавый лом", так же можно найти несколько нормальных материалов "лом" и "образец". Еще можно найти сейф который можно вскрыть на провинутом уровне науки (?)
   — анализ: почти целиком **контент**, и это редкость в списке. Снос уже даёт лут и уборку
   (§12.15), переплавка — обычный рецепт (`recipes:`), «ржавый лом» — запись в `items:`, сейф за
   наукой — тайл с `tech:` (как стеллаж и мастерская). Кодом придётся трогать только стартовую
   застройку: сейчас `build:` в рулсете кладёт готовые комнаты, а нужно поле руин с кучами.
   **Дёшево относительно эффекта: меняет первые полчаса партии, то есть ровно то, что игрок
   видит первым. Сильный кандидат.**