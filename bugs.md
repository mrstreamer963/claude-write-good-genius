## bugs

1. [x] несколько котов могут занимать одну лежанку / гнездо / парту — §12.32, `spread_units`
2. [x] при удалении частичной постройки материалы просто исчезают — §12.31, возврат кучей на клетку
3. [x] юниты могут проходить друг сквозь друга, но не останавливаться в одной точке, но они могут останавливаться на ячейке где лежат материалы или предметы — §12.32
4. [x] закончивший работу юнит должен освобождать ячейку чтобы другой кот мог ей воспользоваться (например поспать на лежанке) — закрыт тем же §12.32: место освобождается, когда на него кто-то претендует (тест `a_freed_bed_is_used_by_the_next_cat`)

## extension
1. [ ] экипирование кота со склада а не автоматом, пусть он идет за экипировкой на склад (или просто поднимает с пола если увидит свободный)
2. [x] кот падает неподалеку от лежанки - может при падении до какого-то критического значения кот идет сам восполнять базовые нужды игнорируя задачи? (можно добавить явный приказ по игнорированию критического порога)
   — подтверждено: упавший на пол спит там до утра, даже если лежанка рядом освободилась
   (`assign_rest` берёт только котов **без** `Rest`). Это пересмотр §12.20.
   — решено §12.33: второй порог бодрости (`critical` в рулсете) срывает кота с работы, но
   только под свободную лежанку; упавший перебирается на лежанку, как только она освободилась;
   «явный приказ» — тумблер «Беречь себя» (`setAutoRest`), отменяющий ровно второй порог.


### extension-ui
1. [x] добавить переключение скорости кнопками space / 1 / 2 / 3 — пробел переключает паузу и возвращает прежний темп; клавиша узнаётся по `code`, поэтому работает в любой раскладке
2. [x] улучшение ux, layout "записка" наслаивается на меню, в меню слишком много пунктов и появляется полоса прокрутки - 
предлагаю вынести основные пункты в главное меню, остальное раскрывать при нажатии на пункт
   — разделы тулбара сворачиваются (открыт один), записка уехала в правую колонку панелей,
   подсказка убирается кнопкой «?»; скролла в меню больше нет