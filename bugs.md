## bugs

1. [x] несколько котов могут занимать одну лежанку / гнездо / парту — §12.32, `spread_units`
2. [x] при удалении частичной постройки материалы просто исчезают — §12.31, возврат кучей на клетку
3. [x] юниты могут проходить друг сквозь друга, но не останавливаться в одной точке, но они могут останавливаться на ячейке где лежат материалы или предметы — §12.32
4. [x] закончивший работу юнит должен освобождать ячейку чтобы другой кот мог ей воспользоваться (например поспать на лежанке) — закрыт тем же §12.32: место освобождается, когда на него кто-то претендует (тест `a_freed_bed_is_used_by_the_next_cat`)
5. [x] если кот шел на строительство а его послали на вылазку - он так и носит материалы с собой и при возвращении с вылазки (бросать материалы там же где стоял и идти на вылазку ?)
   — решено §12.38: ношу кот роняет кучей **под ноги, в момент заявки** — там, где стоял.
   Лом нужен базе сразу (сбор отряда ничем не ограничен), куча ложится по дороге к площадке,
   ради которой его и подняли, а шлюз не засоряется завалом (§12.35 — он замедляет шаг, а шлюз
   самая ходовая клетка). Приказ игрока ношу при этом **не** роняет: кот сходит и донесёт.
6. [x] если кот залезает в стеллаж и единственный выход закрыт другим котом - он так и сидит в стеллаже ( сделать стеллаж вообще непроходимым а перед ним клетку на которой нельзя стоять просто так, а только при использовании стеллажа ? это может повлечь за собой механику вращения ячеек и расширение механики
когда одна сущность (стеллаж) - использует сама несоклько ячеек на карте)
   — решено §12.39: сойти с полки важнее, чем не встать вдвоём. Свободного соседа нет — кот
   сходит на занятого, а наложение разбирает `spread_units` следующим тиком (§12.32 прямо
   называет «стоят вместе» легальным состоянием). Обратная сторона того же порядка: развод
   больше не отходит на заставленную клетку — иначе коты качались бы между полкой и соседом.
   Мебель на несколько клеток с фасадом отложена: она ляжет естественно, когда стеллаж станет
   объектом, но непроходимость отрежет его от носильщиков, которым сдавать груз, стоя на нём.
7. [x] проверить - реализован ли медик и самоисцеление в игре, визуально не наблюдаю
   — реализованы оба (`health.rs`: `assign_heal` · `assign_treat` · `heal`; в рулсете лазарет
   `heal: 6`, `mend: 1`, навык «Медицина»). Не видно было потому, что медик ничем не помечен:
   крест висел только над раненым, и работа медика выглядела как безделье. Решено §12.41:
   над дошедшим медиком — тот же крест контуром, а в карточке кота появилась строка занятия.

## extension
1. [x] экипирование кота со склада а не автоматом, пусть он идет за экипировкой на склад (или просто поднимает с пола если увидит свободный)
   — решено §12.34: надевание стало задачей общего слоя (`Equipping`), кот идёт к ближайшей куче
   с нужной вещью — на складе или прямо на полу — и берёт её оттуда сам. Отряд ждёт одевающегося
   бойца; мгновенное списание со склада осталось только там, где это плата (найм, наука, заказ).
2. [x] кот падает неподалеку от лежанки - может при падении до какого-то критического значения кот идет сам восполнять базовые нужды игнорируя задачи? (можно добавить явный приказ по игнорированию критического порога)
   — подтверждено: упавший на пол спит там до утра, даже если лежанка рядом освободилась
   (`assign_rest` берёт только котов **без** `Rest`). Это пересмотр §12.20.
   — решено §12.33: второй порог бодрости (`critical` в рулсете) срывает кота с работы, но
   только под свободную лежанку; упавший перебирается на лежанку, как только она освободилась;
   «явный приказ» — тумблер «Беречь себя» (`setAutoRest`), отменяющий ровно второй порог.
3. [x] коты не могут оставаться на стеллажах
   — решено §12.35: у тайла появилось свойство `solid` (в рулсете оно у стеллажа). Пройти клетку
   можно, остаться нельзя: свободных и спящих сгоняет `clear_solids`, кто при деле — остаётся
   (носильщик сдаёт груз именно там, строитель работает с соседней клетки).
4. [x] при проходе через кучки предметов - скорость замедляется
   — решено §12.35: шаг на клетку с кучей длится тем дольше, чем куча больше, до потолка.
   Считается только пол — сложенное в склад или на стеллаж не мешает. Маршрут (BFS) о завалах
   не знает: он считает шаги, а не время, иначе коты обходили бы то, что пришли разбирать.
5. [ ] при клике на ячейку где находится кот - игра переходит в режим курсора и выбирается кот (иначе в режиме строительства кликаешь на кота и под ним начинается стройка)

### extension-ui
1. [x] добавить переключение скорости кнопками space / 1 / 2 / 3 — пробел переключает паузу и возвращает прежний темп; клавиша узнаётся по `code`, поэтому работает в любой раскладке
2. [x] улучшение ux, layout "записка" наслаивается на меню, в меню слишком много пунктов и появляется полоса прокрутки - 
предлагаю вынести основные пункты в главное меню, остальное раскрывать при нажатии на пункт
   — разделы тулбара сворачиваются (открыт один), записка уехала в правую колонку панелей,
   подсказка убирается кнопкой «?»; скролла в меню больше нет
3. [x] показывать в карточке кота его текущую работу и надписи "устал", "голоден", "ранен"
   — решено §12.41: занятие считает ядро одним ключом (`Busy::job`) и отдаёт вместе с признаком
   «ещё идёт», а панель называет его словом — «строит», «несёт лом», «идёт есть», «лечит
   раненого». Четыре старых флага (`sleeping` · `studying` · `healing` · `treating`) удалены:
   они были ровно `job == X && !moving`. У бодрости названы оба порога («устал» и «на исходе
   сил»); «голоден» и «ранен» стояли ещё с §12.36 и §12.37.